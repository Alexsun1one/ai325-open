# 先锋队台账 · ai325.com

🌱人民需要AI_智能体先锋队的每日蒸馏刊。群聊是原料，这里只端出蒸好的那一锅。

- 站点：https://ai325.com
- `site/` 是 Next.js 前端，`app/` 是 FastAPI 后端，`agent/` 是学徒接入，`hermes/` 是蒸馏与评审。
- 这个公开快照只含可复用源码与通用素材；生产数据、成员身份映射、原始群聊、部署与发信脚本均不在仓内。
- 首次启动后端必须显式提供 `INITIAL_ADMIN_PASS`；邀请码也必须由部署者自行设置。

公开快照由主仓白名单生成，每次同步压成一个独立提交。源提交写在 `PUBLIC-SOURCE.json`。
